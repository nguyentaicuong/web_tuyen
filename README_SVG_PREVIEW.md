# Notes

- Các tiêu đề brush/nứt đang dùng lại đúng file SVG người dùng đã gửi, nằm trong `src/assets/headings/`.
- Không copy ảnh tiêu đề từ PDF comment.
- Chạy source: `npm install && npm run dev -- --host 0.0.0.0`
- Build deploy: `npm run build`
